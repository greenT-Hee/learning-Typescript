// function sum(a, b) {
//   return a + b;
// }

// sum(); //1020

// // let result = sum(10, 20);
// // 자동완성이 안됨
// // result.toLocaleString();

// @ts-check
// 이렇게 주석처리하면 타스처럼 역할을 해줌

/**
 *
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 * @returns
 */

function sum(a, b) {
  return a + b;
}

// sum(10, "20");
